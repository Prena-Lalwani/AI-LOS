# Frontend build-time config (Vite). Copy to .env.local for local overrides, or
# set VITE_API_BASE as an Environment Variable in Vercel for production.
# Leave unset to default to http://localhost:8000 during local development.
VITE_API_BASE=https://your-backend.onrender.com
